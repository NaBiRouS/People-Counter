global:
  # Prometheus will collect metrics every 15 seconds
  scrape_interval: 15s

scrape_configs:
  # Metrics configuration for the People Counter API
  - job_name: "people-counter-api"

    static_configs:
      - targets:
          - "api:8000"